from setuptools import setup, find_packages
setup(name="plato-hologram", version="0.1.0",
      description="PLATO as vectorized knowledge field — tile = embedding, every tile knows the whole field",
      packages=find_packages(), python_requires=">=3.8")
